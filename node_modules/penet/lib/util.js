var url = require("url");

var util = {};

util.extend = function(target, src, skipArr){
    var skipArr = typeof skipArr == "Array" ? skipArr : [];
    for(k in src){
        if (! _.contains(skipArr, k)) target[k] = src[k];
    }
};

util.clone = function(obj){
    return obj.constructor(obj.valueOf());
}


module.exports = {
    logger: {
        debug: function(message){
            console.debug("[DEBUG] - " + (message || "undefined message"));
        },

        log: function(message){
            console.log("[LOG] - " + (message || "undefined message"));
        },

        info: function(message){
            console.info("[INFO] - " + (message || "undefined message"));
        },

        warn: function(message){
            console.warn("[WARN] - " + (message || "undefined message"));
        },

        error: function(message){
            console.error("[ERROR] - " + (message || "undefined message"));
        }
    }
};
